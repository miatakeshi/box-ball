{
  "name": "box-ball",
  "version": "1.0.0",
  "description": "Homework for digital graphics",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "vite --host",
    "build": "vite build && npm run copy",
    "copy": "cpx 'libraries/*' dist/libraries && cpx sketch.js dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpx": "^1.5.0",
    "vite": "^5.4.11",
    "vite-plugin-static-copy": "^2.1.0"
  }
}
